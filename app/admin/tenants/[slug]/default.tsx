// Required by Next.js App Router to handle client-side navigation
// into and out of the [slug] dynamic segment without hitting a parallel route error.
export default function Default() {
  return null
}
